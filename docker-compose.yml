services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docs
    ports:
      - "8082:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
